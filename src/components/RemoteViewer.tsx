import { useEffect,useRef,useState } from "react";
import type RFB from "@novnc/novnc";
import { normalizeEndpoint,type RemoteConnection } from "../lib/agent-protocol";
type RfbClipboardApi=RFB&{clipboardPasteFrom(text:string):void;addEventListener(type:"clipboard",listener:(event:CustomEvent<{text:string}>)=>void):void};
export function RemoteViewer({remote,endpoint,token:_token,viewOnly,onDisconnect,onError}:{remote:RemoteConnection;endpoint:string;token:string;viewOnly:boolean;onDisconnect:()=>void;onError:(message:string)=>void}){const containerRef=useRef<HTMLDivElement>(null),rfbRef=useRef<RFB|null>(null),disposingRef=useRef(false),viewOnlyRef=useRef(viewOnly),onDisconnectRef=useRef(onDisconnect),onErrorRef=useRef(onError),disconnectTimerRef=useRef<ReturnType<typeof setTimeout>|null>(null);const[loadingModule,setLoadingModule]=useState(true);useEffect(()=>{viewOnlyRef.current=viewOnly},[viewOnly]);useEffect(()=>{onDisconnectRef.current=onDisconnect},[onDisconnect]);useEffect(()=>{onErrorRef.current=onError},[onError]);
useEffect(()=>{const container=containerRef.current;if(!container)return;disposingRef.current=false;let disposed=false,rfb:RFB|null=null;const controlEndpoint=new URL(normalizeEndpoint(endpoint));controlEndpoint.pathname=`/vnc/${remote.sessionId}/${remote.monitorIndex}`;controlEndpoint.search=`ticket=${encodeURIComponent(remote.vncTicket)}`;container.replaceChildren();setLoadingModule(true);const handlePaste=(event:ClipboardEvent)=>{if(viewOnlyRef.current||!event.clipboardData||!rfbRef.current)return;const text=event.clipboardData.getData("text/plain");if(!text)return;const paste=(rfbRef.current as RfbClipboardApi).clipboardPasteFrom;if(!paste)return;event.preventDefault();paste.call(rfbRef.current as RfbClipboardApi,text)};container.addEventListener("paste",handlePaste);let resizeFrame=0;const resizeObserver=new ResizeObserver(()=>{if(resizeFrame)return;resizeFrame=requestAnimationFrame(()=>{resizeFrame=0;if(rfbRef.current===rfb&&rfb)rfb.scaleViewport=true})});resizeObserver.observe(container);void(async()=>{try{const{default:RFBClass}=await import("@novnc/novnc");if(disposed)return;rfb=new RFBClass(container,controlEndpoint.toString(),{credentials:{password:remote.vncPassword}});const clipboardRfb=rfb as RfbClipboardApi;rfb.scaleViewport=true;rfb.resizeSession=false;rfb.viewOnly=viewOnlyRef.current;rfb.showDotCursor=true;rfb.addEventListener("connect",()=>{if(disconnectTimerRef.current){clearTimeout(disconnectTimerRef.current);disconnectTimerRef.current=null}onErrorRef.current("")});clipboardRfb.addEventListener("clipboard",event=>{if(navigator.clipboard)void navigator.clipboard.writeText(event.detail.text).catch(()=>undefined)});rfb.addEventListener("securityfailure",event=>onErrorRef.current(`VNC authentication failed: ${event.detail.reason??"Unknown reason"}`));rfb.addEventListener("disconnect",event=>{if(disposingRef.current)return;if(disconnectTimerRef.current)clearTimeout(disconnectTimerRef.current);disconnectTimerRef.current=setTimeout(()=>{disconnectTimerRef.current=null;if(!disposingRef.current)onDisconnectRef.current()},event.detail.clean?0:500)});rfbRef.current=rfb}catch{if(!disposed)onErrorRef.current("Failed to load the VNC viewer component.")}finally{if(!disposed)setLoadingModule(false)}})();return()=>{disposed=true;disposingRef.current=true;if(disconnectTimerRef.current){clearTimeout(disconnectTimerRef.current);disconnectTimerRef.current=null}container.removeEventListener("paste",handlePaste);resizeObserver.disconnect();if(resizeFrame)cancelAnimationFrame(resizeFrame);if(rfbRef.current===rfb)rfbRef.current=null;if(rfb)try{rfb.disconnect()}catch{}container.replaceChildren()}},[remote.agentId,remote.sessionId,remote.monitorIndex,remote.vncTicket,endpoint]);useEffect(()=>{if(rfbRef.current)rfbRef.current.viewOnly=viewOnly},[viewOnly]);return <div className="relative h-full w-full min-h-0"><div ref={containerRef} className="vnc-surface absolute inset-0"/>{loadingModule&&<div className="absolute inset-0 z-10 flex items-center justify-center bg-black"><p className="text-xs text-muted-foreground">Loading viewer…</p></div>}</div>}

import { memo,useEffect,useRef,useState } from "react";
import { Eye,EyeOff,Maximize2,Minimize2,Monitor,X } from "lucide-react";
import { Button } from "./ui/button";import { Card,CardContent,CardHeader,CardTitle } from "./ui/card";import { Label } from "./ui/label";import { Switch } from "./ui/switch";import { RemoteViewer } from "./RemoteViewer";import { cn } from "../lib/utils";import { connectionKey,sessionMonitors,type AgentConnection,type MonitorInfo,type RemoteConnection } from "../lib/agent-protocol";
const EDGE_BAR_PX=2,EDGE_BAR_LINGER_MS=1500;
export type MonitoringActions={openFullscreen:(key:string)=>void;closeFullscreen:()=>void;setViewOnly:(viewOnly:boolean)=>void;disconnectRemote:(agentId:string,sessionId:string,monitorIndex:number)=>void;startSession:(agentId:string,sessionId:string,monitorIndex:number)=>void;viewerError:(message:string)=>void};
const SessionViewerCard=memo(function SessionViewerCard({agent,session,monitor,remote,connecting,isFullscreen,fullscreenViewOnly,actions}:{agent:AgentConnection;session:AgentConnection["sessions"][number];monitor:MonitorInfo;remote:RemoteConnection|undefined;connecting:boolean;isFullscreen:boolean;fullscreenViewOnly:boolean;actions:MonitoringActions}){const key=connectionKey(agent.id,session.sessionId,monitor.index);const[edgeBarVisible,setEdgeBarVisible]=useState(false);const hideTimerRef=useRef<ReturnType<typeof setTimeout>|null>(null),overChromeRef=useRef(false);const clear=()=>{if(hideTimerRef.current){clearTimeout(hideTimerRef.current);hideTimerRef.current=null}};const hide=()=>{if(overChromeRef.current||hideTimerRef.current)return;hideTimerRef.current=setTimeout(()=>{hideTimerRef.current=null;setEdgeBarVisible(false)},EDGE_BAR_LINGER_MS)};useEffect(()=>{if(!isFullscreen)return;const options={capture:true,passive:true} as const;const move=(e:MouseEvent)=>{if(e.clientY<=EDGE_BAR_PX){clear();setEdgeBarVisible(true)}else hide()};window.addEventListener("mousemove",move,options);return()=>{clear();overChromeRef.current=false;setEdgeBarVisible(false);window.removeEventListener("mousemove",move,options)}},[isFullscreen]);const chrome={onMouseEnter:()=>{overChromeRef.current=true;clear()},onMouseLeave:()=>{overChromeRef.current=false;hide()}};
 return <Card className={cn("group relative flex min-h-0 flex-col overflow-hidden",isFullscreen&&"fixed inset-0 z-50 m-0 rounded-none border-0")}><CardHeader {...chrome} className={cn("absolute inset-x-0 top-0 z-30 flex-row items-center justify-between space-y-0 border-b border-white/10 bg-black/60 px-3 py-2 text-primary-foreground backdrop-blur-sm transition-opacity duration-200",isFullscreen?(edgeBarVisible?"pointer-events-auto opacity-100":"pointer-events-none opacity-0"):"pointer-events-none opacity-0 group-hover:pointer-events-auto group-hover:opacity-100 group-focus-within:pointer-events-auto group-focus-within:opacity-100")}><div className="min-w-0"><CardTitle className="truncate text-sm text-white">{session.username} · {monitor.name}</CardTitle><p className="truncate text-[11px] text-white/60">{agent.identity?.deviceName??agent.endpoint} · Session {session.sessionId} · {monitor.width&&monitor.height?`${monitor.width}×${monitor.height}`:`Monitor ${monitor.index+1}`}{monitor.isPrimary?" · Primary":""}</p></div><div className="flex items-center gap-2">{isFullscreen&&remote&&<><Label className="flex items-center gap-2 text-xs text-white"><Switch checked={fullscreenViewOnly} onCheckedChange={actions.setViewOnly}/>{fullscreenViewOnly?<Eye className="h-4 w-4"/>:<EyeOff className="h-4 w-4"/>}View only</Label></>}{remote&&!isFullscreen&&<Button variant="ghost" size="icon" className="hover:bg-white/15 hover:text-white" onClick={()=>actions.openFullscreen(key)}><Maximize2 className="h-4 w-4"/></Button>}{remote&&<Button variant="ghost" size="icon" className="hover:bg-white/15 hover:text-white" onClick={()=>actions.disconnectRemote(agent.id,session.sessionId,monitor.index)}><X className="h-4 w-4"/></Button>}{isFullscreen&&remote&&<Button variant="outline" size="sm" className="border-white/20 bg-transparent text-white hover:bg-white/15 hover:text-white" onClick={actions.closeFullscreen}><Minimize2 className="h-4 w-4"/> Exit</Button>}</div></CardHeader><CardContent className={cn("relative min-h-0 bg-black p-0 viewer-viewport",isFullscreen&&"rounded-none viewer-fullscreen")}>{remote?<RemoteViewer remote={remote} endpoint={agent.endpoint} token={agent.token} viewOnly={isFullscreen?fullscreenViewOnly:true} onDisconnect={()=>actions.disconnectRemote(agent.id,session.sessionId,monitor.index)} onError={actions.viewerError}/>:<div className="flex h-full w-full items-center justify-center bg-black"><Button size="sm" disabled={agent.status!=="Connected"||connecting} onClick={()=>actions.startSession(agent.id,session.sessionId,monitor.index)}>{connecting?"Connecting…":`Connect ${monitor.name}`}</Button></div>}</CardContent></Card>});
export function MonitoringPage({agents,connectingSessions,connectedByKey,totalSessions,totalMonitors,fullscreenKey,fullscreenViewOnly,globalError,actions}:{agents:AgentConnection[];connectingSessions:Set<string>;connectedByKey:Map<string,RemoteConnection>;totalSessions:number;totalMonitors:number;fullscreenKey:string|null;fullscreenViewOnly:boolean;globalError:string;actions:MonitoringActions}){return <main className="min-w-0 flex-1 overflow-auto"><div className="flex min-h-full flex-col"><div className="border-b px-5 py-4"><p className="text-xs font-medium uppercase tracking-wide text-muted-foreground">Monitoring</p><h1 className="mt-1 text-xl font-semibold">Remote viewers</h1><p className="mt-1 text-sm text-muted-foreground">{agents.length} agent{agents.length===1?"":"s"} · {totalSessions} sessions · {totalMonitors} monitors. Each monitor can connect independently.</p></div>{globalError&&<div className="border-b bg-destructive/10 px-5 py-2.5 text-sm text-destructive">{globalError}</div>}<section className="flex-1 p-5">{totalSessions===0?<div className="flex min-h-90 items-center justify-center rounded-xl border bg-muted/10 text-center"><div><Monitor className="mx-auto mb-3 h-10 w-10 text-muted-foreground/50"/><p className="text-sm font-medium">No remote sessions available</p></div></div>:<div className="viewer-grid">{agents.flatMap(agent=>agent.sessions.flatMap(session=>sessionMonitors(session).map(monitor=>({agent,session,monitor})))).map(({agent,session,monitor})=>{const key=connectionKey(agent.id,session.sessionId,monitor.index);return <SessionViewerCard key={key} agent={agent} session={session} monitor={monitor} remote={connectedByKey.get(key)} connecting={connectingSessions.has(key)} isFullscreen={fullscreenKey===key} fullscreenViewOnly={fullscreenViewOnly} actions={actions}/>})}</div>}</section></div></main>}
